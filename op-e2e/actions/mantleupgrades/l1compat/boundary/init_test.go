// Package boundary contains op-e2e action tests that exercise behavior at
// the L1 Glamsterdam (Amsterdam) upgrade activation boundary — both just
// before the activation block (still in the old format) and across the
// activation point (with L1 reorgs straddling it). Mantle Phase 1.
package boundary
